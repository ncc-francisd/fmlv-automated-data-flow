# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Runtime data — see config/manufacturers.README.md for what stays tracked
/data/snapshots
/data/exports
/data/uploads
/data/run_store.sqlite3

# Local secrets
.env

# Office owner-lock files. Word/Excel create these beside an open document and
# delete them on close, so a tracked one is a permanent phantom change that
# re-modifies itself whenever the document is opened.
~$*
